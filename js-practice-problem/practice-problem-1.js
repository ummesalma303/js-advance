multiply = (num1, num2, num3) => num1 * num2 * num3;
console.log(multiply(7, 4, 2));

//2
const text = `I am a web developer.
  I love to code.
  I love to eat biriyani.
  `;
console.log(text);

// 3
defaultPera = (a, b = 1) => a + b;

const result = defaultPera(5);
//console.log(result)
